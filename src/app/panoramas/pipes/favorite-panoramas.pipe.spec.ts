import { FavoritePanoramasPipe } from './favorite-panoramas.pipe';

describe('FavoritePanoramasPipe', () => {
  it('create an instance', () => {
    const pipe: FavoritePanoramasPipe = new FavoritePanoramasPipe();
    expect(pipe).toBeTruthy();
  });
});
